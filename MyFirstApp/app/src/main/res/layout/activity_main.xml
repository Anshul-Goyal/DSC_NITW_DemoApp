<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/screenBackground"
    tools:context="com.anshul.myfirstapp.MainActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="0"
        android:layout_marginTop="10sp"
        android:textSize="100sp"
        android:textColor="#FFFFFF"
        android:layout_centerHorizontal="true"
        android:id="@+id/textView" />

    <Button
        android:text="TOAST"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/toast_button"
        android:layout_alignBaseline="@+id/count_button"
        android:layout_alignBottom="@+id/count_button"
        android:layout_toLeftOf="@+id/count_button"
        android:layout_toStartOf="@+id/count_button"
        android:background="@color/buttonBackgrund"
        android:onClick="toastMe"
        />

    <Button
        android:text="COUNT"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/count_button"
        android:layout_below="@+id/textView"
        android:layout_centerHorizontal="true"
        android:background="@color/buttonBackgrund"
        android:onClick="countMe"/>

    <Button
        android:text="RANDOM"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/random_button"
        android:layout_toRightOf="@+id/count_button"
        android:layout_toEndOf="@+id/count_button"
        android:layout_alignBottom="@+id/count_button"
        android:background="@color/buttonBackgrund"
        android:onClick="randomMe"/>

</RelativeLayout>
